### Fixed
- A firmware update that gets interrupted — power loss, a stall, or a reboot mid-download — no longer leaves the device stuck restarting into the minimal update screen; it now comes back up in normal firmware, still running the previous version, ready to retry.
