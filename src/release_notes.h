// AUTO-GENERATED from RELEASE_NOTES.md by tools/gen_release_notes.py.
// Do not edit by hand — edit RELEASE_NOTES.md and rebuild.
#pragma once

// Release notes for the build this firmware was cut from, shown by the
// Release Notes entry in the config screen. Empty when no notes were
// available at build time (a plain dev build, typically).
static const char RELEASE_NOTES_TEXT[] = R"CAMNOTES(Changed
- Heltec V4 and Square: every popup and full-screen tool now closes with an X in the top-right corner, in the same place on all of them - Channels, Device Info, Action Result, Help, Release Notes, Bluetooth Keyboard, the emoji tray, Channel Actions, the Tools and Live Filter pickers, Locate, Line of Sight, traceroute progress, the New DM picker, the SNR/RSSI and Channel Utilization charts, Beacons, Discovery and the MQTT monitor - replacing the mix of full-width Close buttons and header Close buttons.
- Heltec V4 and Square: the emoji and symbol tray shows more of the grid, since the corner X no longer takes a row at the foot of the panel.
- Heltec V4 and Square: the Live Filter picker and the Release Notes viewer both gained the height the old Close button was using.
- Heltec V4 and Square: the Bluetooth Keyboard popup now lists only its three actions, with closing handled by the corner X.
- Heltec V4 and Square: the map legend popup is shorter, and the MQTT monitor header has more room for its title.)CAMNOTES";
